# 🐙 Octopus Connect - Interactive Animation

Интерактивное приложение с осьминогом, который оживает когда пользователи добавляют свои никнеймы.

## 🚀 Быстрый старт

### 1. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Для продакшена замените на ваш Railway URL:
# NEXT_PUBLIC_API_URL=https://your-app-name.railway.app/api
```

### 2. Запуск бэкенда

```bash
cd backend
pip install -r requirements.txt
python app.py
```

### 3. Запуск фронтенда

```bash
npm install
npm run dev
```

## 🎯 Функциональность

- **Интерактивный осьминог** с анимированными щупальцами
- **Добавление никнеймов** через форму
- **Проверка дубликатов** в реальном времени
- **Синхронизация с базой данных** PostgreSQL
- **Адаптивный дизайн** для мобильных устройств
- **Космический фон** с красивыми эффектами

## 🛠 Технологии

### Фронтенд:
- Next.js 13+ (App Router)
- TypeScript
- Tailwind CSS
- Framer Motion
- Zustand
- React Spring

### Бэкенд:
- Flask
- PostgreSQL
- SQLAlchemy
- Flask-CORS

## 📁 Структура проекта

```
re_project/
├── app/                    # Next.js App Router
├── components/             # React компоненты
│   ├── Octopus/           # Компоненты осьминога
│   └── ui/                # UI компоненты
├── lib/                   # Утилиты и API клиент
├── backend/               # Flask API
│   ├── app.py            # Основное приложение
│   ├── requirements.txt  # Python зависимости
│   └── Procfile         # Railway конфигурация
└── public/               # Статические файлы
```

## 🚀 Деплой

### Бэкенд на Railway:
1. Подключите GitHub репозиторий
2. Выберите папку `backend`
3. Добавьте PostgreSQL базу данных
4. Railway автоматически задеплоит

### Фронтенд на Vercel:
1. Подключите GitHub репозиторий
2. Установите переменную `NEXT_PUBLIC_API_URL`
3. Vercel автоматически задеплоит

## 🎨 Особенности дизайна

- **Космический фон** с градиентами
- **Анимированные щупальца** с GIF анимацией
- **Живой осьминог** с моргающими глазами и говорящим ртом
- **Черная форма** с логотипом "reCONNECT"
- **Плавные анимации** и переходы

## 🔧 API Endpoints

- `GET /api/users` - получить всех пользователей
- `POST /api/users` - добавить пользователя
- `GET /api/users/{id}` - получить пользователя по ID
- `GET /api/health` - проверка здоровья API

## 📱 Мобильная версия

Приложение полностью адаптивно и работает на всех устройствах:
- Форма центрируется на мобильных
- Анимации оптимизированы для производительности
- Поддержка `prefers-reduced-motion`

## 🎉 Готово к использованию!

Просто запустите бэкенд и фронтенд, и наслаждайтесь интерактивным осьминогом!
